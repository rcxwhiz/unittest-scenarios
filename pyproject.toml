[project]
name = "unittest-scenarios"
version = "0.0.0"
description = ""
authors = [
    {name = "Josh Bedwell",email = "rcxwhiz@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
]

[tool.poetry]
packages = [{include = "unittest_scenarios", from = "src"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
